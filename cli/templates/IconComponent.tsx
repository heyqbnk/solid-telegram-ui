import { withDefaultSize } from './withDefaultSize.js';
import type { IconProps } from './types.js';

export interface $1Props extends IconProps {
}

export const $1 = withDefaultSize((props: $1Props) => ($2), $3);
